# Architecture

UI -> API Route -> Provider -> Store

## Sequence Diagram
User -> Chat UI -> API Route -> NVIDIA Provider -> Stream Response -> UI

## NVIDIA Setup
Set NVIDIA_NIM_API_KEY and NVIDIA_NIM_MODEL in environment.